This tracker has 2 components to its memory:

1. The original image and its bounding box
2. The extremum summary

Here, sample weights are not automatically updated. In contrast to the dimp partitioned tracker, it does
not have the online update set.
